Change List

- Full shell integration (right-click>>check version) for tested files.
- Allow re-run on new file when run as exe (previously had to quit and reload).
- Added support for library files which means the list of supported files now includes:

	-.vi
	-.vit
	-.ctl
	-.ctt
	-.lvproj
	-.lvlib
	-.lvlibp
	-.lvclass
	-.xctl

Known Issues

- Source project does not show registry details in the installer dialog. These have been entered and work, just don't display (fixed in 2012).
